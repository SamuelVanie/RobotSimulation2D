{
  "configurations":{
    "run":{
      "adapter": "debugpy",
      "default": true,
      "configuration": {
        "request": "launch",
        "protocol": "auto",
        "program": "${file}",
        "cwd": "${workspaceRoot}"
      }
    }
  }
}
